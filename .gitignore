node_modules/
.env
.env.local
.vercel

# Raw PNGs extracted from the Bio.design catalogue PDF (~95MB working set).
# Only the curated, optimised subset in assets/img/realizaciones/ is committed.
assets/img/catalogo-raw/
hero-piscina.png
hero-movil.png

# Vaults de Obsidian: notas internas de negocio. Este repositorio es público (GitHub
# Pages no admite repos privados en el plan gratuito), así que los vaults se versionan
# aparte, en su propio repositorio privado. Ver "Vault BIODESIGN/LEEME.md".
#
# ⚠️ Si renombras o creas otro vault, AÑÁDELO AQUÍ ANTES de hacer commit. Estas reglas
# van por nombre exacto de carpeta; un vault sin listar acabaría publicado.
Vault BIODESIGN/
Bio-desing by Apavi Green/
